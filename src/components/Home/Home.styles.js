import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
`;

export const Pun = styled.div`
  text-align: center;
  margin: 20px auto;
  font-family: cursive;
  font-size: var(--fontGiant);
  color: var(--red);

  img {
    width: 10%;
  }
`;
